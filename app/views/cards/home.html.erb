<div class="row">
  <div class="col-lg-12 text-center">
    <h1>Первый в мире удобный менеджер флеш-карточек.</h1>
    <h1>Именно так.</h1>
  </div>
</div>

<% if @card.present? %>
  <div class="row text-center">
    <div class="col-lg-4 col-lg-offset-4 col-md-6 col-md-offset-3">
      <hr>
      <h2>Случайная карточка:</h2>
      <div class="well">
        <h3><%= @card.translated_text %></h3>
        <%= form_tag check_word_cards_path do |f| %>
          <%= hidden_field_tag :id, @card.id %>
          <div class="form-group">
            <%= text_field_tag :original_text, nil, class: "form-control", placeholder: 'Enter original text...' %>
          </div>
          <%= submit_tag "Проверить слово", class: "btn btn-large btn-primary" %>
        <% end %>
      </div>
    </div>
  </div>
<% end %>
